meta {
  name: Login sem CSRF - Bloqueio Esperado
  type: http
  seq: 2
}

post {
  url: http://localhost:8080/clinica/api/login
  body: json
  auth: none
}

headers {
  Accept: application/json
  Content-Type: application/json
}

body:json {
  {
    "email": "tutor@exemplo.com",
    "senha": "senha123",
    "lembrar": false
  }
}

tests {
  test("bloqueio CSRF esperado", function() {
    expect(res.status).to.equal(403);
  });
}
